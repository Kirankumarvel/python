# Example with max and min Functions

# Given string
text = 'Hello world'

# Find the highest character based on ASCII value
big = max(text)
print("The highest character is:", big)

# Find the lowest character based on ASCII value
small = min(text)
print("The lowest character is:", small)